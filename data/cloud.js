/* ============================================================
   云端存档配置
   ------------------------------------------------------------
   部署完 Cloudflare Worker 后，把它给你的网址填到下面。
   形如：https://runrun-camp-save.你的名字.workers.dev

   留空 = 不用云端存档（就还是本地文件 / 浏览器存储）。

   ⚠️ 配对码（密钥）绝对不要写在这个文件里 ——
      这个文件会跟着网页一起发布到 GitHub，谁都能看见。
      配对码是每台设备第一次打开时，由家长在网页上输入一次的。
   ============================================================ */
window.CLOUD = {
  url: 'https://runrun-camp-save.wudapeng-ha.workers.dev',
  profile: null       // 留空就用 PROFILE.name（润润）
};
